# RiftVM: the greeter's Hyprland needs virtio-gpu's cursor plane too; see
# /etc/xdg/uwsm/env-hyprland. Overrides 10-wayland.conf.
[Wayland]
CompositorCommand=env AQ_NO_ATOMIC=1 start-hyprland -- --config /usr/share/sddm/hyprland.lua
